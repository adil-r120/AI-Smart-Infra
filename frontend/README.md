# Frontend (React)

This folder is intended to hold the React dashboard. You can scaffold a new app using Create React App or your preferred tool.

Example (Create React App):

```powershell
cd frontend
npx create-react-app .
npm start
```

Replace this README with project-specific frontend instructions.
